A diagram shows MPI processes reading a global input wave from `wavefiles/2D/2d-tiny-in.wo`, creating partial local arrays using split_range(), and adding halo rows as needed. Each rank stores the header data, data it owns, and the halo values required for computation.
